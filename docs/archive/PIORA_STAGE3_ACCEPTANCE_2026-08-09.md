# Piora 阶段 3 验收记录（2026-08-09）

## 本阶段范围

- 设置页：新增 Codex 风格左侧返回按钮，保留右侧关闭按钮；返回和关闭均回到打开设置前的应用上下文。
- T19 权限与审批：提供只读、自动编辑、完全访问三档语义权限；高风险命令通过 Pi 磁盘扩展进入允许一次、当前任务允许、拒绝的审批闭环。
- T20 命令面板：复用现有 Pi bash 执行与 SSE 流，不引入第二套终端后端；支持历史、终止、重新运行、复制输出与 `!!` 模式。
- T21 桌面体验：补齐窗口位置/最大化恢复、托盘、运行任务数、原生输入上下文菜单和可选全局快捷键；IPC 仍为显式白名单。
- T22 工作区搜索：支持文件名模糊搜索和正文搜索，优先使用 ripgrep，缺失时回退 Node；包含取消、超时、结果上限、忽略规则和文件行定位。

## 自动化结果

- `npm test`：492 项，487 通过、0 失败、5 项因 Windows 权限能力跳过。
- `npm run lint`：通过，0 error、0 warning。
- `npm run typecheck`：Web 与 `@piora/desktop` 均通过。
- 未运行 `next build`，遵循项目开发约束，避免污染 `.next/`。

## 浏览器验收

在 `http://127.0.0.1:30141` 完成以下真实页面检查：

- 设置页左上角存在“返回 Piora”按钮，点击后设置对话页消失并回到原页面。
- 工作区面板存在“审阅 / 文件 / 命令 / 搜索”四个标签，键盘左右键可导航。
- 文件名搜索 `workspace-search` 返回 `lib/workspace-search.ts` 等结果。
- 正文搜索 `WORKSPACE_SEARCH_LIMIT` 返回路径、行号、列号和预览。
- 点击正文结果后切换到文件标签，打开目标文件并将编辑器选区定位到第 22 行。
- 命令面板展示非交互式命令输入、运行按钮、`!!` 选项和交互命令限制说明；验收未执行会修改项目或会话的命令。
- 页面无运行时错误、无告警文本、无横向溢出。

## 尚未执行的发布级验证

本阶段完成了 Electron 源码与类型级验证，但没有生成或启动便携 EXE。便携包、托盘和全局快捷键的真实 Windows 发布包验收应在发布阶段执行；本轮按开发约束没有运行构建流程。
