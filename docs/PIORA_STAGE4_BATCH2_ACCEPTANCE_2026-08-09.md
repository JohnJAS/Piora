# Piora Stage 4 · Batch 2 验收记录

日期：2026-08-09

## 本批范围

- 大型 Diff 渐进式渲染：超过 800 行时首批渲染 400 行，后续按 400 行批次继续加载，并支持进入视口时自动续载。
- 设置页闭环搜索：在设置左侧导航中搜索全部设置分类，结果仍在同一个设置页面内打开，返回按钮回到 Piora 主界面。
- 参数化命令：Ctrl+K 支持 `>rename 任务名`、`>commit 提交说明`，英文及中文别名可解析，Tab 可从别名前缀补全。
- 真实动作接入：重命名参数调用现有任务重命名流程；提交参数只打开审阅面板并预填提交说明，不自动暂存或提交。
- 性能预算：新增 12,000 行 Diff 首批渲染窗口检查。

## 自动化验证

- `npm test`：509 项，504 通过，0 失败，5 跳过。
- `npm run typecheck`：Web 与 Desktop TypeScript 检查通过。
- `npm run lint`：通过。
- `npm run perf:check`：全部预算通过。
  - 500 个任务筛选：0.33ms / 50ms
  - 5,000 文件索引搜索：2.76ms / 800ms
  - 2,000 条聊天窗口选择：0.00ms / 50ms
  - 5,000 文件树首批窗口：0.00ms / 50ms
  - 12,000 行 Diff 首批窗口：0.00ms / 50ms
- `git diff --check`：本批文件通过。

## 浏览器验收

在 `http://127.0.0.1:30141` 的中文界面完成：

1. 打开设置，搜索“模型”，结果在设置页内显示；点击结果后仍只有一个设置对话层，模型配置直接在设置主区域呈现；“返回 Piora”正确回到工作区。
2. Ctrl+K 输入 `>commit 浏览器验收说明`，参数提示正确；输入 `>com` 后 Tab 补全为 `>commit `。
3. 执行参数化提交命令后，审阅面板打开且提交说明被预填；没有执行 Git 提交、暂存或还原。
4. 打开 `components/SessionSidebar.tsx` 的大型 Diff，首屏显示“已显示 400/2369 行，加载更多”，继续加载后仍分批推进。
5. 页面无 Next.js 错误覆盖层、无运行时错误文本、无文档级横向溢出。

## 备注

- 验收期间原开发服务曾无响应，已停止对应的 Piora 开发进程并重新运行 `npm run dev`；当前服务可访问。
- 遵循项目约束，未运行 `next build`。
